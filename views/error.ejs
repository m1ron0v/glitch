<%- include('partials/header') %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="alert alert-danger p-4 shadow-sm" role="alert">
                <h1 class="display-1"><i class="fas fa-bug fa-beat" style="--fa-animation-duration: 2s;"></i></h1>
                <h2 class="alert-heading mt-3">Ой, сталася помилка!</h2>
                <p class="lead"><%= message %></p>
                <hr class="my-4">
                <p>Спробуйте повернутися на головну сторінку або перевірте URL-адресу.</p>
                <a href="/" class="btn btn-primary btn-lg mt-3"><i class="fas fa-home me-2"></i>На головну</a>
            </div>

            <% if (typeof error !== 'undefined' && error && Object.keys(error).length > 0 && (typeof process !== 'undefined' && process.env && process.env.NODE_ENV === 'development')) { %>
                <div class="card mt-4 text-start">
                    <div class="card-header">
                        Деталі помилки (в режимі розробки):
                    </div>
                    <div class="card-body">
                        <pre style="white-space: pre-wrap; word-wrap: break-word;"><%= JSON.stringify(error, null, 2) %></pre>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</div>

<%- include('partials/footer') %>
